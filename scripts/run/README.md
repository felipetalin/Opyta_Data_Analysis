# Runners

Entry points estaveis para executar pipelines e receitas.

- `run_project_recipe.py`: executa recipes declaradas em `configs/projects/`.
- `fauna/`: execucoes multiempreendimento por grupo.
- `meio_fisico/`: runner dedicado ao pipeline de meio fisico.

Para novos projetos, prefira criar uma recipe em vez de duplicar scripts.
